module Engine.Types where

-- Tupel types.
type Tupel     a = (a, a)
type Triple    a = (a, a, a)
type Quadruple a = (a, a, a, a)

-- Generic numeric types.
type Clamp a = a

-- Mathematical concepts.
type Radius a = a
type Angle  a = a
type Steps    = Int
type Offset a = a
type Width  a = a
type Height a = a
type Index    = Int

